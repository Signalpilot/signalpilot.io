User-agent: *
Allow: /

# Old URL pattern (/lang/articles/) now has redirect pages
# Allow crawling so Google can follow the meta refresh redirects

# Filter parameter URLs handled via canonical tags and noindex meta tags
# (not blocked here so Google can see the canonical/noindex directives)

Sitemap: https://www.signalpilot.io/blog/sitemap.xml
Sitemap: https://www.signalpilot.io/blog/feed.xml
